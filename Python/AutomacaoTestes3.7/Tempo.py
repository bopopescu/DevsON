# -*- coding: utf-8 -*-
import autoit
import time
import log
import Tempo
import Variaveis


def Brek():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 10)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def Click():
    #Variaveis.Tempo = (15.0 / 100.0)
    Variaveis.Tempo = ( Variaveis.TempoAdd + 0.25)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def ClickFinalVenda():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def Dig():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 0.40)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def Sql():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)

def DigProd():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 0.10)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)



def Esc():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 0.25)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def EnterProduto():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 0.15)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def Enter():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 0.15)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TeclaAcaoFinalVenda():
    Variaveis.Tempo = ( Variaveis.TempoAddFinalVenda + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)

def F8FinalVenda():
    Variaveis.Tempo = ( Variaveis.TempoAddFinalVenda + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TeclaAcao():
    Variaveis.Tempo = ( Variaveis.TempoAddFinalVenda + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def F8TelaLancamento():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)

def TelaAcao():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TelaLancamento():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 2)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)



def TelaFinalVenda():
    Variaveis.Tempo = ( Variaveis.TempoAddFinalVenda + 2 )
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def Loop():
    Variaveis.Tempo = ( Variaveis.TempoAddFinalVenda + 0.10 )
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)



def TelaAlertaCrt():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TelaAlerta():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 5)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TratamentoRejeicao():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 5)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)



def TempoR():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 1)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TempoM():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 3)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TempoL():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 5)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)


def TempoLL():
    Variaveis.Tempo = ( Variaveis.TempoAdd + 10)
    log.EscreverLog('Aguardar ate ' + str(Variaveis.Tempo) + ' segundos')
    time.sleep(Variaveis.Tempo)